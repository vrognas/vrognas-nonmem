// Copy this file to .vscode/settings.json and adjust to taste.
// settings.json is gitignored so per-developer config stays local.
//
// All connection details (HostName, User, Port, IdentityFile, ProxyJump, ...)
// come from your ~/.ssh/config. The extension shells out to the system ssh
// client, so whatever `ssh <alias>` does in your terminal, the extension
// will do too.
{
  "positronNonmem.host.alias": "primary"
}
